<Window x:Class="ADBFastbootGUI.Windows.DialogBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:ADBFastbootGUI.Windows" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="DialogBox"
        Height="125"
        AllowsTransparency="True"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Loaded="Window_Loaded" ResizeMode="NoResize" ShowInTaskbar="False" SizeToContent="Width" WindowStartupLocation="CenterOwner" WindowStyle="None"
        mc:Ignorable="d">

    <Window.Resources>
        <Style x:Key="ExitButton" TargetType="Button">
            <Setter Property="Width" Value="30" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E81123" />
                                <!--  Kırmızı  -->
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C50F1F" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" CornerRadius="20" GlassFrameThickness="0" />
    </WindowChrome.WindowChrome>

    <Grid Margin="20">
        <TextBlock x:Name="Message"
                   Margin="0,-20,0,0" Padding="10" HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="16" />
        <!--  MouseDown="TopPanel_MouseDown"  -->
        <StackPanel Width="275"
                    Margin="-235,-20,-20,0" HorizontalAlignment="Right" VerticalAlignment="Top"
                    Background="Transparent" MouseDown="StackPanel_MouseDown" Orientation="Horizontal" RenderTransformOrigin="0.5,0.5">

            <StackPanel.RenderTransform>
                <TransformGroup>
                    <ScaleTransform ScaleX="-1" />
                    <SkewTransform />
                    <RotateTransform />
                    <TranslateTransform />
                </TransformGroup>
            </StackPanel.RenderTransform>
            <!--  Click="CloseButton_Click"  -->
            <Button x:Name="CloseButton"
                    Click="CloseButton_Click" Content="X"
                    Style="{StaticResource ExitButton}" />
        </StackPanel>

        <Button x:Name="OKButton"
                Margin="0,0,-12,-12" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Click="OKButton_Click" Content="OK" />
        <Button x:Name="CancelButton"
                Margin="0,0,48,-12" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Click="CancelButton_Click" Content="Cancel" />

    </Grid>
</Window>
